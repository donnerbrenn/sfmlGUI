{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Rebuild all",
            "type": "shell",
            "command": "make clean && time make gui -j14 && ./gui",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}